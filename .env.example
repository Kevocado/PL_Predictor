ODDS_API_KEY=
FOOTBALL_DATA_KEY=

# Only needed for the public, password-gated deployment (see README's
# "Deploying a public, read-only version") — leave both unset for local/
# private use, which is the default everywhere and disables this entirely.
# PUBLIC_MODE=true
# GUEST_PASSWORD=
